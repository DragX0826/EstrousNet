# Error Analysis

Use this folder to keep review artifacts for failure cases.

- `misclassified_images/`: copies of wrongly predicted images with notes.
- Add a CSV table linking `image_id`, `true_stage`, `pred_stage`, and error reason.

