# Experiments

- `exp1_cell_count`: rule-based baseline using segmentation + ratio rules.
- `exp2_cnn_classifier`: optional cell-patch CNN extension.

Keep each experiment folder self-contained with config, metrics, and notes.

